import React, {Component} from "react";
import {Text, View} from "react-native";

const SubProfile = (props: any) => (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
        <Text>SubProfile</Text>
    </View>
);

export default SubProfile;
